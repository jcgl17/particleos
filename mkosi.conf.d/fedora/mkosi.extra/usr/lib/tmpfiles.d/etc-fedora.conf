# SPDX-License-Identifier: LGPL-2.1-or-later

# Required to source dbus drop-in configurations from other packages like wpa_supplicant
L? /etc/dbus-1
# Required by by podman, at least for policy.json
L? /etc/containers
